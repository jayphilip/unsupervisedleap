---
interface Props {
  title: string;
  description?: string;
  date: Date;
  slug: string;
  tags?: string[];
  categories?: string[];
  readingTime?: number;
  featured?: boolean;
}

const { title, description, date, slug, categories = [], readingTime, featured = false } = Astro.props;
const formattedDate = date.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'short',
  day: 'numeric',
});
---

<article class={`post-entry${featured ? ' post-entry--featured' : ''}`}>
  <header class="entry-header">
    {categories.length > 0 && (
      <p class="entry-eyebrow">
        {categories.map((cat, i) => (
          <>
            <a href={`/categories/${cat.toLowerCase().replace(/ /g, '-')}/`}>{cat}</a>
            {i < categories.length - 1 && <span class="eyebrow-sep"> · </span>}
          </>
        ))}
      </p>
    )}
    <h2>
      <a href={`/posts/${slug}/`}>
        {title}
        <span class="entry-arrow" aria-hidden="true">→</span>
      </a>
    </h2>
  </header>
  {description && (
    <div class="entry-content">
      <p>{description}</p>
    </div>
  )}
  <footer class="entry-footer">
    <time datetime={date.toISOString()}>{formattedDate}</time>
    {readingTime && <span>· {readingTime} min read</span>}
  </footer>
</article>
